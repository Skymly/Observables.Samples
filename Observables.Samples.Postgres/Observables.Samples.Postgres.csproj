<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Observables.Samples.Postgres</RootNamespace>
    <ObservablesSampleKind>Postgres</ObservablesSampleKind>
    <Description>Console sample for Observables.Postgres R3 LISTEN/NOTIFY proxy generator (no live server in CI).</Description>
  </PropertyGroup>

</Project>
