<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Observables.Samples.Nats</RootNamespace>
    <ObservablesSampleKind>Nats</ObservablesSampleKind>
    <Description>Console sample for Observables.Nats R3 subject proxy generator (no live server in CI).</Description>
  </PropertyGroup>

</Project>
